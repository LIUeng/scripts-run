export const Configuration = {
  // run background
  terminal: 'showTerminal',
  // tree map
  tree: 'treeMap',
  // cache scripts
  cache: 'cache',
  // exclude search
  exclude: 'excludeFiles',
  // nvm where
  whichNvm: 'whichNvm',
};